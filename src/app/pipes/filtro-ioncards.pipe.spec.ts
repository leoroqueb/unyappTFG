import { FiltroIoncardsPipe } from './filtro-ioncards.pipe';

describe('FiltroIoncardsPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroIoncardsPipe();
    expect(pipe).toBeTruthy();
  });
});
